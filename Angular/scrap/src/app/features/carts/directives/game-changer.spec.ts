import { GameChanger } from './game-changer';

describe('GameChanger', () => {
  it('should create an instance', () => {
    const directive = new GameChanger();
    expect(directive).toBeTruthy();
  });
});
