import { Highlighter } from './highlighter';

describe('Highlighter', () => {
  it('should create an instance', () => {
    const directive = new Highlighter();
    expect(directive).toBeTruthy();
  });
});
